[project]
name = "renderrob"
version = "3.5.12"
description = "Render Manager"
readme = "README.md"
authors = [{ name = "Peter Baintner", email = "pip@dreisicht.net" }]
requires-python = ">=3.13"
dependencies = [
    "bpy",
    "setuptools",
    "py2app>=0.28.8",
    "protobuf>=6.33.0",
    "py2appsigner>=0.6.95",
    "pyside6-essentials>=6.10.0",
]

[dependency-groups]
# protoc ships inside grpcio-tools, so the proto bindings can be generated from the project's
# own environment on every platform instead of needing a system protoc install.
dev = [
    "grpcio-tools>=1.76.0",
    "ruff>=0.16.0",
    # ty is pre-1.0 and gains rules with every release, so pin it exactly: a floor would turn a
    # green branch red on somebody else's release rather than on a change made here.
    "ty==0.0.79",
]
# Only the release workflow and a local `pyinstaller tools/renderrob.spec` need this, so it is
# kept out of `dev` - nobody running the tests should have to download it.
build = ["pyinstaller>=6.16.0"]

[build-system]
requires = ["setuptools>=64", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
include = ["proj*"]

[tool.ruff]
# Exclude a variety of commonly ignored directories.
exclude = [
    ".bzr",
    ".direnv",
    ".eggs",
    ".git",
    ".git-rewrite",
    ".hg",
    ".ipynb_checkpoints",
    ".mypy_cache",
    ".nox",
    ".pants.d",
    ".pyenv",
    ".pytest_cache",
    ".pytype",
    ".ruff_cache",
    ".svn",
    ".tox",
    ".venv",
    ".vscode",
    "__pypackages__",
    "_build",
    "buck-out",
    "build",
    "dist",
    "node_modules",
    "site-packages",
    "venv",
]

# Same as Black.
line-length = 100
indent-width = 2

target-version = "py313"

[tool.ruff.lint]
# Enable Pyflakes (`F`) and a subset of the pycodestyle (`E`) codes by default.
# Unlike Flake8, Ruff doesn't enable pycodestyle warnings (`W`) or
# McCabe complexity (`C901`) by default.
select = [
    "PLE",
    "EM",
    "LOG",
    "PLR",
    "TRY",
    "ASYNC",
    "PIE",
    "N",
    "BLE",
    "SIM",
    "PYI",
    "W",
    "RSE",
    "ERA",
    "SLOT",
    "D",
    "YTT",
    "DJ",
    "S",
    "T10",
    "INT",
    "ICN",
    "ISC",
    "FLY",
    "PERF",
    "TC",
    "PTH",
    "EXE",
    "COM",
    "SLF",
    "F",
    "FAST",
    "UP",
    "ANN",
    "DTZ",
    "TD",
    "FA",
    "ARG",
    "E",
    "T20",
    "TID",
    "FIX",
    "RET",
    "NPY",
    "PD",
    "PLC",
    "C4",
    "FBT",
    "C90",
    "Q",
    "G",
    "I",
    "AIR",
    "PGH",
    "PL",
    "PLW",
    "FURB",
    "B",
    "PT",
    "RUF",
    "INP",
    "A",
    "CPY",
]
ignore = [
    "D10",    # Missing docstring in publics.
    "PT009",  # Don't use assert functions of testing class.
    "FIX002", #
    "TD003",  # Missing link in todo.
    "S603",   # Check subprocess.run input.
    "CPY001",
    # Render Rob deliberately launches the platform's own tools (open, xdg-open, codesign,
    # nvidia-smi) and the user-configured Blender by name, so they resolve on PATH.
    "S607",
    # Qt and bpy take booleans positionally throughout (setChecked(True), blockSignals(True),
    # view_layer.use = True). We don't control those signatures.
    "FBT003",
    # Conflicts with `ruff format`, which owns trailing commas.
    "COM812",
]

# Allow fix for all enabled rules (when `--fix`) is provided.
fixable = ["ALL"]
unfixable = []

# Allow unused variables when underscore-prefixed.
dummy-variable-rgx = "^(_+|(_+[a-zA-Z0-9_]*[a-zA-Z0-9]+?))$"

[tool.ruff.format]
# Like Black, use double quotes for strings.
quote-style = "double"

# Like Black, indent with spaces, rather than tabs.
indent-style = "space"

# Like Black, respect magic trailing commas.
skip-magic-trailing-comma = false

# Like Black, automatically detect the appropriate line ending.
line-ending = "auto"

# Enable auto-formatting of code examples in docstrings. Markdown,
# reStructuredText code/literal blocks and doctests are all supported.
#
# This is currently disabled by default, but it is planned for this
# to be opt-out in the future.
docstring-code-format = false

# Set the line length limit used when formatting code snippets in
# docstrings.
#
# This only has an effect when the `docstring-code-format` setting is
# enabled.
docstring-code-line-length = "dynamic"

[tool.ruff.lint.pydocstyle]
convention = "google"

# Since there are some issues with gdal needing to be compiled using C++, it can be
# worked around by installing it from sources where pre-built binaries are existent.
[tool.uv.sources]
gdal = [
    { index = "gdal-wheels", marker = "sys_platform == 'linux'" },
    { index = "geospatial_wheels", marker = "sys_platform == 'win32'" },
]

[[tool.uv.index]]
name = "geospatial_wheels"
url = "https://nathanjmcdougall.github.io/geospatial-wheels-index/"
explicit = true

[[tool.uv.index]]
name = "gdal-wheels"
url = "https://gitlab.com/api/v4/projects/61637378/packages/pypi/simple"
explicit = true

[tool.ruff.lint.per-file-ignores]
# Qt dispatches event handlers and delegate hooks by name, so these cannot be renamed to
# snake_case without silently unhooking them.
"src/main.py" = ["N802"]
"src/utils_rr/dropwidget.py" = ["N802"]
"src/utils_rr/table_utils.py" = ["N802"]
# These modules exist to write to the console.
"src/utils_common/print_utils.py" = ["T201"]
"src/utils_common/rr_c_image.py" = ["T201"]
# Tests build protobuf fixtures by hand and assert on booleans; annotating every helper and
# routing every literal through a constant would bury what each test actually checks.
"*_test.py" = ["ANN", "PLR2004", "PLR0915", "S101"]

[tool.ty.environment]
# The app imports its own modules as top-level names (`import main`, `from utils_rr import ...`),
# so ty needs the same source root the test runner uses.
root = ["src"]

[tool.ty.rules]
# Render Rob builds most of its object graph at runtime, in three ways ty cannot follow:
#   * PySide6's "forgiveness mode" enums - Qt.AlignCenter, QMessageBox.Yes, QFile.ReadOnly. They
#     exist at runtime, but the stubs only declare the scoped spellings (Qt.AlignmentFlag.*).
#   * bpy, which has no stubs at all and resolves everything through Blender's RNA at runtime.
#   * The widgets in window.ui and settings.ui, which QUiLoader attaches to a plain QWidget by
#     the object names in the .ui file - self.window.tableWidget and friends.
# Together those are almost every occurrence, so the rule is off rather than annotated away at
# hundreds of call sites. Note what this gives up: it is the rule that caught StateSaver's
# undeclared `parent_widget`. Declare instance attributes in __init__ and it stays caught.
unresolved-attribute = "ignore"

# Qt widgets do not exist until setup() runs, so main.py holds them as None until then, and
# every use afterwards looks like a use of None.
[[tool.ty.overrides]]
include = ["src/main.py"]
[tool.ty.overrides.rules]
invalid-argument-type = "ignore"
invalid-assignment = "ignore"

# Everything reached through bpy.context and bpy.data is untyped, so assigning to a render
# setting looks like assigning to an attribute of None.
[[tool.ty.overrides]]
include = ["src/utils_bpy/**"]
[tool.ty.overrides.rules]
invalid-assignment = "ignore"

# The tests drive the same untyped Qt and bpy objects as the code above.
[[tool.ty.overrides]]
include = ["src/*_test*.py"]
[tool.ty.overrides.rules]
invalid-argument-type = "ignore"
not-iterable = "ignore"

# cx_Freeze is only needed for the Windows build, so it is not a project dependency and cannot
# be resolved here.
[[tool.ty.overrides]]
include = ["tools/setup.py"]
[tool.ty.overrides.rules]
unresolved-import = "ignore"
